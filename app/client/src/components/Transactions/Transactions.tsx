import React from 'react';
import classes from './Transactions.module.css';

const Transactions: React.FC = () => {
  return <h3 className={classes.caption}>Transactions</h3>;
};

export default Transactions;
