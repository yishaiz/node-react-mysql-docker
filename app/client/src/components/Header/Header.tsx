import React from 'react';
import classes from './Header.module.css';

const Header: React.FC = () => {
  return <h3 className={classes.caption}>Customers Transactions</h3>;
};

export default Header;
